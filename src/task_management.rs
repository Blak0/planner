// use std::collections::{HashMap, VecDeque};

// use crate::deserialize::Job;
// use itertools::Itertools;

// pub fn topological_sort_tasks(tasks: HashMap<usize, Job>) -> Vec<Job> {
//     let mut queue = VecDeque::new();
//     for (idx, task) in &tasks {

//     }
// }


// pub fn sort_by_priority_descending(tasks: Vec<Job>) -> Vec<Job> {
//     let mut tasks = tasks;
//     tasks.sort_by(|first, second| second.priority.cmp(&first.priority));
//     tasks
// }
